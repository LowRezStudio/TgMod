class PComMusicThemeFactory extends Factory
    transient
    native(Audio)
    config(Engine);

defaultproperties
{
    SupportedClass=Class'PComMusicTheme'
    Description="Music Theme"
    bCreateNew=true
    bEditAfterNew=true
}
