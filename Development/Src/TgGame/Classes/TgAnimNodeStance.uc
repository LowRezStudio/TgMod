class TgAnimNodeStance extends AnimNodeBlendList
    native(Anim)
    hidecategories(Object,Object,Object,Object)
    config(Engine);

var () int m_Stance;

defaultproperties
{
    Children=/* Array type was not detected. */
    bFixNumChildren=true
}
