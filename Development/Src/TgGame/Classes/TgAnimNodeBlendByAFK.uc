class TgAnimNodeBlendByAFK extends TgAnimNodeBlendList
    native(Anim)
    hidecategories(Object,Object,Object,Object)
    config(Engine);

var TgPawn PawnOwner;

defaultproperties
{
    Children=/* Array type was not detected. */
    bFixNumChildren=true
}
