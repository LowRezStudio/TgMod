class TgAnimNodeLog extends AnimNodeBlendList
    native(Anim)
    hidecategories(Object,Object,Object,Object)
    config(Engine);

defaultproperties
{
    Children=/* Array type was not detected. */
}
