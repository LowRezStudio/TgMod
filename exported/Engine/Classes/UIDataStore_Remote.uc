class UIDataStore_Remote extends UIDataStore
    abstract
    transient
    native(UIPrivate)
    config(Engine)
    hidecategories(Object,UIRoot);

defaultproperties
{
    Tag="RemoteData"
}